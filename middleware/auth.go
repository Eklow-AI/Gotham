package middleware

import "gorm"

// CheckToken validates the given token is valid
func CheckToken(token string) {
	
}

// CheckTokenPriveleges returns the security clearance of a token
func CheckTokenPriveleges(token string) int {

}
